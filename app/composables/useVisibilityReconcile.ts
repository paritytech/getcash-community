// The requests plugin handles visibility now: the store's lifecycle listeners evict the chain
// clients and resume the polls on a return from background. Kept only until its call sites go.

export function useVisibilityReconcile(): void {}
